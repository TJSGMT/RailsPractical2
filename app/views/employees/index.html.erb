<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body><br><br>
<div class="container">
    <div class="d-flex  align-items-center  justify-content-center mb-3">
      <h1 class="text-light"> Employee's Index</h1>
    </div>
  <table class="table  table-hover">
    <thead>
    <tr>
      <th scope="col">Employee Name</th>
      <th scope="col">Email</th>
      <th scope="col">Gender</th>
      <th scope="col">Hobbies</th>
      <th scope="col">Address</th>
      <th scope="col">Phone no.</th>
      <th scope="col">Birth Date</th>
    </tr>
  </thead>
  <tbody>
    <% @employees.each do |employee|%>
    <tr> 
      <th scope="row"><%=employee.employee_name%></th>
      <td><%=employee.email%></td>
      <td><%=employee.gender%></td>
      <td><%=employee.hobbies%></td>
      <td><%=employee.address%></td>
      <td><%=employee.mobile_number%></td>
      <td><%=employee.birtd_date%></td>
      <td><button type="button" class="btn btn-link"><%= link_to "Show", employee_path(employee), class:"btn btn-success", method: :get %></button>
        <button type="button" class="btn btn-link"><%= link_to " Edit", edit_employee_path(employee), class:"btn btn-warning", method: :get %></button>
        <button type="button" class="btn btn-link"><%= button_to " Destroy", employee_path(employee), class:"btn btn-danger", method: :delete %></button>
      </td>
    </tr>
    <%end%> 
  </tbody>
  </table><br>                                      
    <button type="button " class="btn btn-link"><%= link_to "Add new Employee", new_employee_path, class:"btn btn-secondary", method: :get %></button> 
    <button type="button " class="btn btn-link"><%= link_to "Search Employee", search_path, class:"btn btn-secondary", method: :get %></button> 
</div>
</body>
</html>
